version: '3.4'

services:
  php:
    environment:
      APP_ENV: test
    extra_hosts:
      - 'api.assets.ci.ds:${PROXY_HOST}'
      - 'api.authentication.ci.ds:${PROXY_HOST}'
      - 'api.camunda.ci.ds:${PROXY_HOST}'
      - 'api.cases.ci.ds:${PROXY_HOST}'
      - 'api.cms.ci.ds:${PROXY_HOST}'
      - 'api.discovery.ci.ds:${PROXY_HOST}'
      - 'api.formio.ci.ds:${PROXY_HOST}'
      - 'api.forms.ci.ds:${PROXY_HOST}'
      - 'api.identities.ci.ds:${PROXY_HOST}'
      - 'api.microservice.ci.ds:${PROXY_HOST}'
      - 'api.records.ci.ds:${PROXY_HOST}'
      - 'api.services.ci.ds:${PROXY_HOST}'
      - 'api.tasks.ci.ds:${PROXY_HOST}'
      - 'api.tenants.ci.ds:${PROXY_HOST}'
