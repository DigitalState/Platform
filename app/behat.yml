default:
  calls:
    error_reporting: 16383 # E_ALL & ~E_USER_DREPRECATED

  suites:
    default:
      paths:
        - %paths.base%/features
      contexts:
        - FeatureContext
        - ConsoleContext
