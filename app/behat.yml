default:
  calls:
    error_reporting: 16383 # E_ALL & ~E_USER_DREPRECATED

  suites:
    default:
      paths:
        - %paths.base%/features
      filters:
        tags: '~@enterprise'
      contexts:
        - FeatureContext
        - ConsoleContext
    enterprise:
      paths:
        - %paths.base%/features
      contexts:
        - FeatureContext
        - ConsoleContext
