;monitor
;admin
;portal
;api="docker-compose up -d"
;discovery="docker-compose up -d"
identities="docker-compose up -d"
authentication="docker-compose up -d"
;cases="docker-compose up -d"
;assets="docker-compose up -d"
;records="docker-compose up -d"
services="docker-compose up -d"
;topics="docker-compose up -d"
;tasks="docker-compose up -d"
;interactions="docker-compose up -d"
;formio="docker-compose up -d"
;camunda="docker-compose up -d"
;log="docker-compose up -d"
;core
;documentation
;guide
