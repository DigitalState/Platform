---
- name: Load services database fixtures
  shell: 'docker-compose -f /srv/service/services/docker-compose.yml exec php bin/console doctrine:fixtures:load --env={{ fixtures }} --no-interaction | cat'
