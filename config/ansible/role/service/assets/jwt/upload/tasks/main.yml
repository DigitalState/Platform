---
- name: Upload assets jwt private key
  copy:
    src: '{{ jwt_private_key }}'
    dest: /srv/service/assets/var/jwt/key
    mode: 0600

- name: Upload assets jwt public key
  copy:
    src: '{{ jwt_public_key }}'
    dest: /srv/service/assets/var/jwt/key.pub
    mode: 0600
