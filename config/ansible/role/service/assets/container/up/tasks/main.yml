---
- name: Up assets container
  shell: 'docker-compose -f /srv/service/assets/docker-compose.yml up -d'
  register: output
  changed_when: output.stderr != 'assets_db_1 is up-to-date\nassets_php_1 is up-to-date\nassets_nginx_1 is up-to-date'
