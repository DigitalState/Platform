---
- name: Migrate identities database
  when: database_action == 'migrate'
  shell: 'docker-compose -f {{ identities_directory }}/docker-compose.yml exec php bin/console doctrine:migrations:migrate --no-interaction | cat'
