---
- name: Upload records jwt private key
  copy:
    src: '{{ jwt_private_key }}'
    dest: /srv/service/records/var/jwt/key
    mode: 0600

- name: Upload records jwt public key
  copy:
    src: '{{ jwt_public_key }}'
    dest: /srv/service/records/var/jwt/key.pub
    mode: 0600
