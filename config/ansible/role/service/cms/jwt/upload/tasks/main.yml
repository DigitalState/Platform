---
- name: Upload cms jwt private key
  copy:
    src: '{{ jwt_private_key }}'
    dest: /srv/service/cms/var/jwt/key
    mode: 0600

- name: Upload cms jwt public key
  copy:
    src: '{{ jwt_public_key }}'
    dest: /srv/service/cms/var/jwt/key.pub
    mode: 0600
