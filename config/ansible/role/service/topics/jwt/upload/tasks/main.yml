---
- name: Upload topics jwt private key
  copy:
    src: '{{ jwt_private_key }}'
    dest: /srv/service/topics/var/jwt/key
    mode: 0600

- name: Upload topics jwt public key
  copy:
    src: '{{ jwt_public_key }}'
    dest: /srv/service/topics/var/jwt/key.pub
    mode: 0600
