---
- name: Up topics container
  shell: 'docker-compose -f /srv/service/topics/docker-compose.yml up -d'
  register: output
  changed_when: output.stderr != 'topics_db_1 is up-to-date\ntopics_php_1 is up-to-date\ntopics_nginx_1 is up-to-date'
