---
- name: Download authentication repository
  git:
    repo: https://github.com/DigitalState/Authentication.git
    version: '{{ authentication_version }}'
    dest: '{{ authentication_directory }}'
