---
- name: Up authentication container
  shell: 'docker-compose -f /srv/service/authentication/docker-compose.yml up -d'
  register: output
  changed_when: output.stderr != 'authentication_db_1 is up-to-date\nauthentication_php_1 is up-to-date\nauthentication_nginx_1 is up-to-date'
