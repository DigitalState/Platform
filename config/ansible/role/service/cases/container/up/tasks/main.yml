---
- name: Up cases container
  shell: 'docker-compose -f /srv/service/cases/docker-compose.yml up -d'
  register: output
  changed_when: output.stderr != 'cases_db_1 is up-to-date\ncases_php_1 is up-to-date\ncases_nginx_1 is up-to-date'
