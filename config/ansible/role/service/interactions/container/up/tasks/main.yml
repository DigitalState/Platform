---
- name: Up interactions container
  shell: 'docker-compose -f /srv/service/interactions/docker-compose.yml up -d'
  register: output
  changed_when: output.stderr != 'interactions_db_1 is up-to-date\ninteractions_php_1 is up-to-date\ninteractions_nginx_1 is up-to-date'
