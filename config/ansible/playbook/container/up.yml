---
- name: Up containers on lab server
  hosts: server
  become: true
  vars:
    container_action: up
  roles:
    - service/authentication/container
    - service/identities/container
    - service/services/container
