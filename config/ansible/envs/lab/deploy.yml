---
- name: Install core packages on lab server
  hosts: server
  become: true
  roles:
    - packages/git
    - packages/docker

- name: Install microservices on lab server
  hosts: server
  become: true
  vars:
    authentication_repository_version: develop
  roles:
    - microservices/authentication
