---
- name: Install ansible requirements
  hosts: server
  become: true
  gather_facts: false
  tasks:
      - name: Install python packages
        include_role:
            name: package/python
  tags: [system]
