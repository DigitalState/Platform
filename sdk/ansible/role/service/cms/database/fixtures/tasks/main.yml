---
- name: Load cms database fixtures
  shell: 'docker-compose -f /srv/service/cms/docker-compose.yml exec cms_php bin/console doctrine:fixtures:load --env={{ fixtures }} --fixtures=/srv/api-platform/src/AppBundle/Fixtures/ORM --no-interaction | cat'
