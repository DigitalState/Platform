---
- name: Set assets config
  shell: 'docker-compose -f /srv/service/assets/docker-compose.yml exec assets_php bin/console ds:config:set {{ item.key }} {{ item.value }} | cat'
  with_dict: '{{ config }}'
