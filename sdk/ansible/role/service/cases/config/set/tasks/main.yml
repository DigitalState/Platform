---
- name: Set cases config
  shell: 'docker-compose -f /srv/service/cases/docker-compose.yml exec cases_php bin/console ds:config:set {{ item.key }} {{ item.value }} | cat'
  with_dict: '{{ config }}'
