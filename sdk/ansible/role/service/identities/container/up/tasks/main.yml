---
- name: Up identities container
  shell: 'docker-compose -f /srv/service/identities/docker-compose.yml up -d'
  register: output
  changed_when: output.stderr != 'identities_db_1 is up-to-date\nidentities_php_1 is up-to-date\nidentities_nginx_1 is up-to-date'
