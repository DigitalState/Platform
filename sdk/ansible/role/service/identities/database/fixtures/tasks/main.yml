---
- name: Load identities database fixtures
  shell: 'docker-compose -f /srv/service/identities/docker-compose.yml exec php bin/console doctrine:fixtures:load --env={{ fixtures }} --no-interaction | cat'
