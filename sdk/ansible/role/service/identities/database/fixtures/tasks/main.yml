---
- name: Load identities database fixtures
  shell: 'docker-compose -f /srv/service/identities/docker-compose.yml exec identities_php bin/console doctrine:fixtures:load --env={{ fixtures }} --fixtures=/srv/api-platform/src/AppBundle/Fixtures/ORM --no-interaction | cat'
