---
- name: Set identities config
  shell: 'docker-compose -f /srv/service/identities/docker-compose.yml exec identities_php bin/console ds:config:set {{ item.key }} {{ item.value }} | cat'
  with_dict: '{{ config }}'
