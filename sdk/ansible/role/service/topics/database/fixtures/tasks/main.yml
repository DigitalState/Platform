---
- name: Load topics database fixtures
  shell:
      cmd: 'docker-compose exec php bin/console doctrine:fixtures:load --env={{ fixtures }} --no-interaction | cat'
      chdir: /srv/service/topics
