---
- name: Set services config
  shell:
      cmd: 'docker-compose exec services_php bin/console ds:config:set {{ item.key }} {{ item.value }} | cat'
      chdir: /srv/service/services
  with_dict: '{{ config }}'
