---
- name: Up services container
  shell: 'docker-compose -f /srv/service/services/docker-compose.yml up -d'
  register: output
  changed_when: output.stderr != 'services_db_1 is up-to-date\nservices_php_1 is up-to-date\nservices_nginx_1 is up-to-date'
