---
- name: Set records config
  shell:
      cmd: 'docker-compose exec records_php bin/console ds:config:set {{ item.key }} {{ item.value }} | cat'
      chdir: /srv/service/records
  with_dict: '{{ config }}'
