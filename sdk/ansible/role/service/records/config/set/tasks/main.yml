---
- name: Set records config
  shell: 'docker-compose -f /srv/service/records/docker-compose.yml exec records_php bin/console ds:config:set {{ item.key }} {{ item.value }} | cat'
  with_dict: '{{ config }}'
