---
- name: Up records container
  shell: 'docker-compose -f /srv/service/records/docker-compose.yml up -d'
  register: output
  changed_when: output.stderr != 'records_db_1 is up-to-date\nrecords_php_1 is up-to-date\nrecords_nginx_1 is up-to-date'
