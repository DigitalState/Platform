---
- name: Load records database fixtures
  shell: 'docker-compose -f /srv/service/records/docker-compose.yml exec php bin/console doctrine:fixtures:load --env={{ fixtures }} --no-interaction | cat'
