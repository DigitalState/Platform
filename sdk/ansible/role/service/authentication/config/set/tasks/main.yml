---
- name: Set authentication config
  shell: 'docker-compose -f /srv/service/authentication/docker-compose.yml exec authentication_php bin/console ds:config:set {{ item.key }} {{ item.value }} | cat'
  with_dict: '{{ config }}'
