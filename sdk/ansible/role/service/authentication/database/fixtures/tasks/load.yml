---
- name: Load authentication database fixtures
  shell:
      cmd: 'docker-compose exec authentication_php bin/console doctrine:fixtures:load --env={{ fixtures }} --fixtures=/srv/api-platform/src/AppBundle/Fixtures/ORM --no-interaction | cat'
      chdir: /srv/service/authentication
