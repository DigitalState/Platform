---
- name: Migrate authentication database
  shell:
      cmd: docker-compose exec authentication_php bin/console doctrine:migrations:migrate --no-interaction | cat
      chdir: /srv/service/authentication
