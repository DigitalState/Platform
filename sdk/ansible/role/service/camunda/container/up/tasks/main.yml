---
- name: Up camunda container
  shell:
      cmd: docker-compose up -d
      chdir: /srv/service/camunda
  register: output
  changed_when: output.stderr != 'camunda_camunda_camunda_1 is up-to-date'
