---
- name: Set interactions config
  shell: 'docker-compose -f /srv/service/interactions/docker-compose.yml exec interactions_php bin/console ds:config:set {{ item.key }} {{ item.value }} | cat'
  with_dict: '{{ config }}'
