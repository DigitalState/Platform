---
- name: Load interactions database fixtures
  shell: 'docker-compose -f /srv/service/interactions/docker-compose.yml exec php bin/console doctrine:fixtures:load --env={{ fixtures }} --no-interaction | cat'
