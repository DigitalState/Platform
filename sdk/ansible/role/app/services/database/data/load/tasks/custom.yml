---
- name: Backup services fixtures directory
  shell: 'cp -R /srv/services/src/AppBundle/Resources/data/{{ data }} /srv/services/src/AppBundle/Resources/data/_{{ data }}'

- name: Download custom services fixtures directory
  copy:
      src: '/root/resource/data/{{ data }}/services/'
      dest: '/srv/services/src/AppBundle/Resources/data/{{ data }}'

- import_tasks: load.yml

# The restore backup task below runs before docker-compose above finishes executing. Look into fixing this.
- name: Pause while services fixtures load
  pause:
      seconds: 15

- name: Delete custom services fixtures directory
  file:
      path: '/srv/services/src/AppBundle/Resources/data/{{ data }}'
      state: absent

- name: Restore services fixtures directory backup
  shell: 'mv /srv/services/src/AppBundle/Resources/data/_{{ data }} /srv/services/src/AppBundle/Resources/data/{{ data }}'
