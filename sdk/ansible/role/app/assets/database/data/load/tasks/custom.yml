---
- name: Backup assets fixtures directory
  shell: 'cp -R /srv/assets/src/AppBundle/Resources/data/{{ data }} /srv/assets/src/AppBundle/Resources/data/_{{ data }}'

- name: Download custom assets fixtures directory
  copy:
      src: '/root/resource/data/{{ data }}/assets/'
      dest: '/srv/assets/src/AppBundle/Resources/data/{{ data }}'

- import_tasks: load.yml

# The restore backup task below runs before docker-compose above finishes executing. Look into fixing this.
- name: Pause while assets fixtures load
  pause:
      seconds: 15

- name: Delete custom assets fixtures directory
  file:
      path: '/srv/assets/src/AppBundle/Resources/data/{{ data }}'
      state: absent

- name: Restore assets fixtures directory backup
  shell: 'mv /srv/assets/src/AppBundle/Resources/data/_{{ data }} /srv/assets/src/AppBundle/Resources/data/{{ data }}'
