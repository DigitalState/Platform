---
- name: Load cms database fixtures
  shell:
      cmd: 'docker-compose exec php php bin/console doctrine:fixtures:load --env={{ cms_data }} --fixtures=/srv/api-platform/src/AppBundle/Fixtures/ORM --no-interaction | cat'
      chdir: /srv/cms
