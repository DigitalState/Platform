---
- block:
      - name: Define tasks database variables
        set_fact:
            tasks_data: '{{ data }}'
      - name: Load tasks database fixtures
        include_role:
            name: app/tasks/database/data/load
  when:
      - tasks.enabled
      - data is not none
  tags: [tasks]
