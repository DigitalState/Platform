---
- block:
      - name: Define services database variables
        set_fact:
            services_data: '{{ data }}'
      - name: Load services database fixtures
        include_role:
            name: app/services/database/data/load
  when:
      - services.enabled
      - data is not none
  tags: [services]
