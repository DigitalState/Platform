---
- block:
      - name: Define cases database variables
        set_fact:
            cases_data: '{{ data }}'
      - name: Load cases database fixtures
        include_role:
            name: app/cases/database/data/load
  when:
      - cases.enabled
      - data is not none
  tags: [cases]
