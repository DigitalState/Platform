---
- block:
      - name: Define interactions database variables
        set_fact:
            interactions_data: '{{ data }}'
      - name: Load interactions database fixtures
        include_role:
            name: app/interactions/database/data/load
  when:
      - interactions.enabled
      - data is not none
  tags: [interactions]
