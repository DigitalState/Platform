---
- block:
      - name: Define assets database variables
        set_fact:
            assets_data: '{{ data }}'
      - name: Load assets database fixtures
        include_role:
            name: app/assets/database/data/load
  when:
      - assets.enabled
      - data is not none
  tags: [assets]
