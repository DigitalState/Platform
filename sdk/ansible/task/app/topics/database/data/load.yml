---
- block:
      - name: Define topics database variables
        set_fact:
            topics_data: '{{ data }}'
      - name: Load topics database fixtures
        include_role:
            name: app/topics/database/data/load
  when:
      - topics.enabled
      - data is not none
  tags: [topics]
