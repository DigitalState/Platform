---
- block:
      - name: Define cms database variables
        set_fact:
            cms_data: '{{ data }}'
      - name: Load cms database fixtures
        include_role:
            name: app/cms/database/data/load
  when:
      - cms.enabled
      - data is not none
  tags: [cms]
