---
- block:
    - name: Stop interactions container
      docker_service:
        project_src: /srv/interactions
        files:
          - docker-compose.yml
          - 'docker-compose.{{ interactions_env }}.yml'
        state: present
        stopped: yes
  tags: [interactions]
