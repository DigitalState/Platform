---
- block:
    - name: Down interactions container
      docker_service:
        project_src: /srv/interactions
        files:
          - docker-compose.yml
          - 'docker-compose.{{ interactions_env }}.yml'
        state: absent
        remove_volumes: '{{ "no" if interactions_persistent else "yes" }}'
  tags: [interactions]
