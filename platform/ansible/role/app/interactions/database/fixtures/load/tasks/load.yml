---
- name: Load interactions database fixtures
  shell:
    cmd: 'docker-compose exec -T php php bin/console doctrine:fixtures:load --env={{ interactions_fixtures }} --fixtures=/srv/api-platform/src/AppBundle/Fixtures --no-interaction | cat'
    chdir: /srv/interactions

- name: Pause while interactions fixtures load
  pause:
    seconds: 15
