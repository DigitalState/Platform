---
- block:
    - name: Clear interactions cache
      shell:
        cmd: 'docker-compose exec -T php php bin/console cache:clear --no-warmup --env={{ interactions_env }}'
        chdir: /srv/interactions
  tags: [interactions]
