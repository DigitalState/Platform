---
- block:
    - name: Set interactions repository active branch
      git:
        repo: '{{ _repository }}'
        version: '{{ _branch }}'
        dest: /srv/interactions
        update: no
  tags: [interactions]
