---
- block:
    - name: Set portal repository active branch
      git:
        repo: '{{ _repository }}'
        version: '{{ _branch }}'
        dest: /srv/portal
        update: no
  tags: [portal]
