---
- block:
    - name: Clear cases cache
      shell:
        cmd: 'docker-compose exec -T php php bin/console cache:clear --no-warmup --env={{ cases_env }}'
        chdir: /srv/cases
  tags: [cases]
