---
- block:
    - name: Warmup authentication cache
      shell:
        cmd: 'docker-compose exec -T php php bin/console cache:warmup --env={{ authentication_env }}'
        chdir: /srv/authentication
  tags: [authentication]
