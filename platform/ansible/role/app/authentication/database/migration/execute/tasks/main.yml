---
- block:
    - name: Execute authentication database migration
      shell:
        cmd: 'docker-compose exec -T php php bin/console doctrine:migrations:migrate {{ _version }} --no-interaction'
        chdir: /srv/authentication
    - name: Pause while authentication database migration executes
      pause:
        seconds: 15
  tags: [authentication]
