---
- block:
    - name: Load microservice database fixtures
      shell:
        cmd: 'docker-compose exec -T -e FIXTURES={{ _fixtures }} php php bin/console doctrine:fixtures:load --no-interaction'
        chdir: /srv/microservice
    - name: Pause while microservice fixtures load
      pause:
        seconds: 15
  tags: [microservice]
