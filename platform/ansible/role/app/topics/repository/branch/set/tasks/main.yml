---
- block:
    - name: Set topics repository active branch
      git:
        repo: '{{ _repository }}'
        version: '{{ _branch }}'
        dest: /srv/topics
        update: no
  tags: [topics]
