---
- block:
    - name: Execute cms database migration
      shell:
        cmd: 'docker-compose exec -T php php bin/console doctrine:migrations:migrate {{ _version }} --no-interaction'
        chdir: /srv/cms
    - name: Pause while cms database migration executes
      pause:
        seconds: 15
  tags: [cms]
