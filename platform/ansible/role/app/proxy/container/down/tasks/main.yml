---
- block:
    - name: Down proxy container
      docker_service:
        project_src: /srv/proxy
        files:
          - docker-compose.yml
          - 'docker-compose.{{ proxy_env }}.yml'
        state: absent
        remove_volumes: '{{ "no" if proxy_persistent else "yes" }}'
  tags: [proxy]
