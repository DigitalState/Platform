---
- block:
    - name: Stop logs container
      docker_service:
        project_src: /srv/logs
        files:
          - docker-compose.yml
          - 'docker-compose.{{ _env }}.yml'
        state: present
        stopped: yes
  tags: [logs]
