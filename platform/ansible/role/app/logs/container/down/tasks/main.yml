---
- block:
    - name: Down logs container
      docker_service:
        project_src: /srv/logs
        files:
          - docker-compose.yml
          - 'docker-compose.{{ env }}.yml'
        state: absent
        remove_volumes: '{{ "no" if logs_persistent else "yes" }}'
  tags: [logs]
