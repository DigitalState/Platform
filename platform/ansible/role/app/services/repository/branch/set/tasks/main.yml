---
- block:
    - name: Set services repository active branch
      git:
        repo: '{{ _repository }}'
        version: '{{ _branch }}'
        dest: /srv/services
        update: no
  tags: [services]
