---
- block:
    - name: Set monitor repository active branch
      git:
        repo: '{{ _repository }}'
        version: '{{ _branch }}'
        dest: /srv/monitor
        update: no
  tags: [monitor]
