---
- block:
    - name: Download monitor repository
      git:
        repo: '{{ repository }}'
        version: '{{ version }}'
        recursive: yes
        dest: /srv/monitor
        force: yes
  tags: [monitor]
