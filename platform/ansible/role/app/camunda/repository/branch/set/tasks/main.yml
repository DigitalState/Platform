---
- block:
    - name: Set camunda repository active branch
      git:
        repo: '{{ _repository }}'
        version: '{{ _branch }}'
        dest: /srv/camunda
        update: no
  tags: [camunda]
