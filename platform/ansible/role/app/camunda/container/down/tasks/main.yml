---
- block:
    - name: Down camunda container
      docker_service:
        project_src: /srv/camunda
        files:
          - docker-compose.yml
          - 'docker-compose.{{ camunda_env }}.yml'
        state: absent
        remove_volumes: '{{ "no" if camunda_persistent else "yes" }}'
  tags: [camunda]
