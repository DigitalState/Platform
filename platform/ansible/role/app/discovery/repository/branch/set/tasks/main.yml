---
- block:
    - name: Set discovery repository active branch
      git:
        repo: '{{ _repository }}'
        version: '{{ _branch }}'
        dest: /srv/discovery
        update: no
  tags: [discovery]
