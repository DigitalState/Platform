---
- block:
    - name: Load tenants database fixtures
      import_tasks: default.yml
      when: tenants_override == ""
    - name: Load tenants database fixtures override
      import_tasks: override.yml
      when: tenants_override != ""
  tags: [tenants]
