---
- block:
    - name: Down tenants container
      docker_service:
        project_src: /srv/tenants
        files:
          - docker-compose.yml
          - 'docker-compose.{{ tenants_env }}.yml'
        state: absent
        remove_volumes: '{{ "no" if tenants_persistent else "yes" }}'
  tags: [tenants]
