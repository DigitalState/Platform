---
- block:
    - name: Set tenants repository active branch
      git:
        repo: '{{ _repository }}'
        version: '{{ _branch }}'
        dest: /srv/tenants
        update: no
  tags: [tenants]
