---
- block:
    - name: Download tenants repository
      git:
        repo: '{{ tenants_repository if tenants_credential is none else tenants_repository|replace("//", "//"+tenants_credential+"@") }}'
        version: '{{ tenants_version }}'
        dest: /srv/tenants
        force: yes
  tags: [tenants]
