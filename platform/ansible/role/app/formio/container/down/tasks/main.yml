---
- block:
    - name: Down formio container
      docker_service:
        project_src: /srv/formio
        files:
          - docker-compose.yml
          - 'docker-compose.{{ env }}.yml'
        state: absent
        remove_volumes: '{{ "no" if formio_persistent else "yes" }}'
  tags: [formio]
