---
- block:
    - name: Load forms database migration
      shell:
        cmd: 'docker-compose exec -T php php bin/console doctrine:migrations:migrate --no-interaction'
        chdir: /srv/forms
    - name: Pause while forms database migration loads
      pause:
        seconds: 15
  tags: [forms]
