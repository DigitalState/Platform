---
- block:
    - name: Download forms repository
      git:
        repo: '{{ forms_repository }}'
        version: '{{ forms_version }}'
        dest: /srv/forms
        force: yes
  tags: [forms]
