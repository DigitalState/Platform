---
- block:
    - name: Set records repository active branch
      git:
        repo: '{{ _repository }}'
        version: '{{ _branch }}'
        dest: /srv/records
        update: no
  tags: [records]
