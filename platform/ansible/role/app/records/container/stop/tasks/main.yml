---
- block:
    - name: Stop records container
      docker_service:
        project_src: /srv/records
        files:
          - docker-compose.yml
          - 'docker-compose.{{ records_env }}.yml'
        state: present
        stopped: yes
  tags: [records]
