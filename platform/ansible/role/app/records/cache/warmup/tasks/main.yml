---
- block:
    - name: Warmup records cache
      shell:
        cmd: 'docker-compose exec -T php php bin/console cache:warmup --env={{ records_env }}'
        chdir: /srv/records
  tags: [records]
