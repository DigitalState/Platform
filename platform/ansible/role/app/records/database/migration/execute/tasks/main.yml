---
- block:
    - name: Execute records database migration
      shell:
        cmd: 'docker-compose exec -T php php bin/console doctrine:migrations:migrate {{ _version }} --no-interaction'
        chdir: /srv/records
    - name: Pause while records database migration executes
      pause:
        seconds: 15
  tags: [records]
