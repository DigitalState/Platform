---
- block:
    - name: Set identities repository active branch
      git:
        repo: '{{ _repository }}'
        version: '{{ _branch }}'
        dest: /srv/identities
        update: no
  tags: [identities]
