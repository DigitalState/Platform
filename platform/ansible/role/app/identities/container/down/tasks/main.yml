---
- block:
    - name: Down identities container
      docker_service:
        project_src: /srv/identities
        files:
          - docker-compose.yml
          - 'docker-compose.{{ identities_env }}.yml'
        state: absent
        remove_volumes: '{{ "no" if identities_persistent else "yes" }}'
  tags: [identities]
