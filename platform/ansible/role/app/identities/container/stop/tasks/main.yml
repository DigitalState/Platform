---
- block:
    - name: Stop identities container
      docker_service:
        project_src: /srv/identities
        files:
          - docker-compose.yml
          - 'docker-compose.{{ identities_env }}.yml'
        state: present
        stopped: yes
  tags: [identities]
