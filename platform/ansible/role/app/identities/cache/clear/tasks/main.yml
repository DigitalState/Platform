---
- block:
    - name: Clear identities cache
      shell:
        cmd: 'docker-compose exec -T php php bin/console cache:clear --no-warmup --env={{ identities_env }}'
        chdir: /srv/identities
  tags: [identities]
