---
- block:
    - name: Execute identities database migration
      shell:
        cmd: 'docker-compose exec -T php php bin/console doctrine:migrations:migrate {{ _version }} --no-interaction'
        chdir: /srv/identities
    - name: Pause while identities database migration executes
      pause:
        seconds: 15
  tags: [identities]
