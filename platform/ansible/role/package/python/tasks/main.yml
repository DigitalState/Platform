---
- name: Detect remote OS
  raw: cat /etc/os-release
  register: _os
  changed_when: false

- debug:
    var: _os

- import_tasks: alpine.yml
  when:
    - _os.stdout.find('ID=alpine') != -1
    - env != 'test'

- import_tasks: ubuntu.yml
  when:
    - _os.stdout.find('ID=ubuntu') != -1
    - env != 'test'

- name: Gather facts
  setup:
