---
tasks:
  enabled: false
  repository: https://github.com/DigitalState/Tasks.git
  version: develop
  api:
    host: api.tasks.ci.ds
  database:
    host: tasks_database
    port: 5432
    name: tasks
    username: tasks
    password: tasks
    ui:
      host: ui.database.tasks.ci.ds
  cache:
    ui:
      host: ui.cache.tasks.ci.ds
