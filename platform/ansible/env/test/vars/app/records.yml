---
records:
  enabled: true
  repository: https://github.com/DigitalState/Records.git
  version: develop
  api:
    host: api.records.test.ds
  database:
    host: records_database
    name: records
    user: records
    password: records
    ui:
      host: ui.database.records.test.ds
  mock:
    discovery:
      host: discovery.mock.records.test.ds
  docker:
    - docker-compose.yml
    - docker-compose.test.yml
    - /root/docker/records/docker-compose.yml
    - /root/docker/records/docker-compose.test.yml
    - /root/docker/api.test.yml
    - /root/docker/php.test.yml
