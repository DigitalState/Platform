---
cases:
  enabled: true
  repository: https://github.com/DigitalState/Cases.git
  version: develop
  api:
    host: api.cases.dev.ds
  database:
    host: cases_database
    name: cases
    user: cases
    password: cases
    ui:
      host: ui.database.cases.dev.ds
  docker:
    - docker-compose.yml
    - docker-compose.dev.yml
    - /root/docker/dns.dev.yml
    - /root/docker/cases.yml
