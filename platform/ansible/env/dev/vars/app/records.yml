---
records:
  enabled: true
  repository: https://github.com/DigitalState/Records.git
  version: develop
  api:
    host: api.records.dev.ds
  database:
    host: records_database
    name: records
    username: records
    password: records
    ui:
      host: ui.database.records.dev.ds
  docker:
    - docker-compose.yml
    - docker-compose.dev.yml
    - /root/docker/dev.yml
    - /root/docker/records.yml
