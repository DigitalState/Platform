---
tasks:
  enabled: true
  repository: https://github.com/DigitalState/Tasks.git
  version: develop
  api:
    host: api.tasks.dev.ds
  database:
    host: tasks_database
    name: tasks
    username: tasks
    password: tasks
    ui:
      host: ui.database.tasks.dev.ds
  docker:
    - docker-compose.yml
    - docker-compose.dev.yml
    - /root/docker/dev.yml
    - /root/docker/tasks.yml
