---
tenants:
  enabled: true
  repository: https://github.com/DigitalState/Tenants.git
  version: develop
  api:
    host: api.tenants.dev.ds
  database:
    host: tenants_database
    name: tenants
    username: tenants
    password: tenants
    ui:
      host: ui.database.tenants.dev.ds
  docker:
    - docker-compose.yml
    - docker-compose.dev.yml
    - /root/docker/dns.dev.yml
    - /root/docker/tenants.yml
