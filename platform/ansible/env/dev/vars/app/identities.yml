---
identities:
  enabled: true
  repository: https://github.com/DigitalState/Identities.git
  version: develop
  api:
    host: api.identities.dev.ds
  database:
    host: identities_database
    name: identities
    username: identities
    password: identities
    ui:
      host: ui.database.identities.dev.ds
  docker:
    - docker-compose.yml
    - docker-compose.dev.yml
    - /root/docker/dns.dev.yml
    - /root/docker/identities.yml
