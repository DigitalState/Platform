---
forms:
  enabled: true
  repository: https://github.com/DigitalState/Forms.git
  version: develop
  api:
    host: api.forms.ci.ds
  database:
    host: forms_database
    port: 5432
    name: forms
    username: forms
    password: forms
    ui:
      host: ui.database.forms.ci.ds
  cache:
    ui:
      host: ui.cache.forms.ci.ds
