---
forms:
  enabled: true
  repository: https://github.com/DigitalState/Forms.git
  version: 0.10.0
  api:
    host: api.forms.stag.ds
  database:
    host: forms_database
    port: 5432
    name: forms
    username: forms
    password: ~
    ui:
      host: ui.database.forms.stag.ds
  cache:
    ui:
      host: ui.cache.forms.stag.ds
