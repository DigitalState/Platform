- name: ...
  stat:
      path: '{{ item }}/{{ tag }}/{{ task }}.yml'
  register: _result

- name: ...
  set_fact:
      files: '{{ files }} + [ "{{ result.stat.path }}" ]'
  when: _result.stat.exists == true
