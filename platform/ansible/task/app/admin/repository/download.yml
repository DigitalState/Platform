---
- block:
    - name: Define admin repository variables
      set_fact:
        admin_repository: '{{ app.admin.repository }}'
        admin_version: '{{ app.admin.version }}'
    - name: Download admin repository
      include_role:
        name: app/admin/repository/download
  when: app.admin.enabled
  tags: [admin]
