---
- block:
    - name: Define admin container variables
      set_fact:
        admin_env: '{{ platform.env }}'
    - name: Stop admin container
      include_role:
        name: app/admin/container/stop
  when: app.admin.enabled
  tags: [admin]
