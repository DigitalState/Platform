---
- block:
    - name: Load tasks database fixtures
      include_role:
        name: app/tasks/database/fixtures/load
      vars:
        _fixtures: '{{ fixtures }}'
  when:
    - fixtures is not none
    - app.tasks.enabled
  tags: [tasks]
