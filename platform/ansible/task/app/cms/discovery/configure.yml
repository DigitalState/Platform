---
- block:
    - name: Define cms discovery variables
      set_fact:
        cms_discovery_host: '{{ app.discovery.api.host }}'
    - name: Configure cms discovery settings
      include_role:
        name: app/cms/discovery/configure
  when: app.cms.enabled
  tags: [cms]
