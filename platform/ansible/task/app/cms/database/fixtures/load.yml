---
- block:
    - name: Load cms database fixtures
      include_role:
        name: app/cms/database/fixtures/load
      vars:
        _fixtures: '{{ fixtures }}'
  when:
    - fixtures is not none
    - app.cms.enabled
  tags: [cms]
