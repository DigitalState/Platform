---
- block:
    - name: Define cms encryption variables
      set_fact:
        cms_encryption_secret: '{{ platform.encryption.secret }}'
    - name: Configure cms encryption settings
      include_role:
        name: app/cms/encryption/configure
  when: app.cms.enabled
  tags: [cms]
