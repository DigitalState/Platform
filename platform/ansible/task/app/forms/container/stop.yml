---
- block:
    - name: Define forms container variables
      set_fact:
        forms_env: '{{ platform.env }}'
    - name: Stop forms container
      include_role:
        name: app/forms/container/stop
  when: app.forms.enabled
  tags: [forms]
