---
- block:
    - name: Load forms database fixtures
      include_role:
        name: app/forms/database/fixtures/load
      vars:
        _fixtures: '{{ fixtures }}'
  when:
    - fixtures is not none
    - app.forms.enabled
  tags: [forms]
