---
- block:
    - name: Define authentication container variables
      set_fact:
        authentication_env: '{{ platform.env }}'
    - name: Down authentication container
      include_role:
        name: app/authentication/container/down
  when: app.authentication.enabled
  tags: [authentication]
