---
- block:
    - name: Define authentication repository variables
      set_fact:
        authentication_repository: '{{ app.authentication.repository }}'
        authentication_version: '{{ app.authentication.version }}'
    - name: Download authentication repository
      include_role:
        name: app/authentication/repository/download
  when: app.authentication.enabled
  tags: [authentication]
