---
- block:
    - name: Define authentication jwt variables
      set_fact:
        authentication_jwt_key_pass_phrase: '{{ platform.jwt.pass_phrase }}'
    - name: Configure authentication jwt key pass phrase
      include_role:
        name: app/authentication/jwt/configure
  when: app.authentication.enabled
  tags: [authentication]
