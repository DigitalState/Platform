---
- block:
    - name: Define interactions container variables
      set_fact:
        interactions_env: '{{ platform.env }}'
    - name: Stop interactions container
      include_role:
        name: app/interactions/container/stop
  when: app.interactions.enabled
  tags: [interactions]
