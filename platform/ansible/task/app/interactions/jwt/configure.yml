---
- block:
    - name: Define interactions jwt variables
      set_fact:
        interactions_jwt_key_pass_phrase: '{{ platform.jwt.pass_phrase }}'
    - name: Configure interactions jwt key pass phrase
      include_role:
        name: app/interactions/jwt/configure
  when: app.interactions.enabled
  tags: [interactions]
