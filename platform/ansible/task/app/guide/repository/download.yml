---
- block:
    - name: Define guide repository variables
      set_fact:
        guide_repository: '{{ app.guide.repository }}'
        guide_version: '{{ app.guide.version }}'
    - name: Download guide repository
      include_role:
        name: app/guide/repository/download
  when: app.guide.enabled
  tags: [guide]
