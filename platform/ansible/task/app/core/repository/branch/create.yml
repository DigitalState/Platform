---
- block:
    - name: Create core repository branch
      include_role:
        name: app/core/repository/branch/create
      vars:
        _branch: '{{ branch }}'
  when: app.core.enabled
  tags: [core]
