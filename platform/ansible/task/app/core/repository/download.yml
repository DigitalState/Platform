---
- block:
    - name: Define core repository variables
      set_fact:
          core_repository: '{{ app.core.repository }}'
          core_version: '{{ app.core.version }}'
    - name: Download core repository
      include_role:
          name: app/core/repository/download
  when: app.core.enabled
  tags: [core]
