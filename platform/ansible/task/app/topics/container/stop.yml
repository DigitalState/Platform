---
- block:
    - name: Define topics container variables
      set_fact:
        topics_env: '{{ platform.env }}'
    - name: Stop topics container
      include_role:
        name: app/topics/container/stop
  when: app.topics.enabled
  tags: [topics]
