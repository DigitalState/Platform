---
- block:
    - name: Define topics repository variables
      set_fact:
        topics_repository: '{{ app.topics.repository }}'
        topics_version: '{{ app.topics.version }}'
    - name: Download topics repository
      include_role:
        name: app/topics/repository/download
  when: app.topics.enabled
  tags: [topics]
