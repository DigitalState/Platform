---
- block:
    - name: Define cases repository variables
      set_fact:
        cases_repository: '{{ app.cases.repository }}'
        cases_version: '{{ app.cases.version }}'
    - name: Download cases repository
      include_role:
        name: app/cases/repository/download
  when: app.cases.enabled
  tags: [cases]
