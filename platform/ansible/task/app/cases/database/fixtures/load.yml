---
- block:
    - name: Load cases database fixtures
      include_role:
        name: app/cases/database/fixtures/load
      vars:
        _fixtures: '{{ fixtures }}'
  when:
    - fixtures is not none
    - app.cases.enabled
  tags: [cases]
