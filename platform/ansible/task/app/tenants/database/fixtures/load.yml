---
- block:
    - name: Load tenants database fixtures
      include_role:
        name: app/tenants/database/fixtures/load
      vars:
        _fixtures: '{{ fixtures }}'
  when:
    - fixtures is not none
    - app.tenants.enabled
  tags: [tenants]
