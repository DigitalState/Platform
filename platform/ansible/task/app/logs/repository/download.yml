---
- block:
    - name: Define logs repository variables
      set_fact:
        logs_repository: '{{ app.logs.repository }}'
        logs_version: '{{ app.logs.version }}'
    - name: Download logs repository
      include_role:
        name: app/logs/repository/download
  when: app.logs.enabled
  tags: [logs]
