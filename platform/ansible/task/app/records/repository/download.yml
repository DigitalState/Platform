---
- block:
    - name: Define records repository variables
      set_fact:
        records_repository: '{{ app.records.repository }}'
        records_version: '{{ app.records.version }}'
    - name: Download records repository
      include_role:
        name: app/records/repository/download
  when: app.records.enabled
  tags: [records]
