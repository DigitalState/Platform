---
- block:
    - name: Define monitor container variables
      set_fact:
        monitor_env: '{{ platform.env }}'
    - name: Stop monitor container
      include_role:
        name: app/monitor/container/stop
  when: app.monitor.enabled
  tags: [monitor]
