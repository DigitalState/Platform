---
- block:
    - name: Define monitor repository variables
      set_fact:
        monitor_repository: '{{ app.monitor.repository }}'
        monitor_version: '{{ app.monitor.version }}'
    - name: Download monitor repository
      include_role:
        name: app/monitor/repository/download
  when: app.monitor.enabled
  tags: [monitor]
