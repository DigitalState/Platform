---
- block:
    - name: Configure microservice jwt key pass phrase
      include_role:
        name: app/microservice/jwt/configure
      vars:
        _pass_phrase: '{{ jwt.pass_phrase }}'
  when: app.microservice.enabled
  tags: [microservice]
