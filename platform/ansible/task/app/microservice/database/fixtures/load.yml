---
- block:
    - name: Load microservice database fixtures
      include_role:
        name: app/microservice/database/fixtures/load
      vars:
        _fixtures: '{{ fixtures }}'
  when: app.microservice.enabled
  tags: [microservice]
