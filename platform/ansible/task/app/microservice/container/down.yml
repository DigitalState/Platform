---
- block:
    - name: Down microservice container
      include_role:
        name: app/microservice/container/down
      vars:
        _env: '{{ env }}'
  when: app.microservice.enabled
  tags: [microservice]
