---
- block:
    - name: Load assets database fixtures
      include_role:
        name: app/assets/database/fixtures/load
      vars:
        _fixtures: '{{ fixtures }}'
  when:
    - fixtures is not none
    - app.assets.enabled
  tags: [assets]
