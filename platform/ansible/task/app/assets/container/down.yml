---
- block:
    - name: Define assets container variables
      set_fact:
        assets_env: '{{ platform.env }}'
    - name: Down assets container
      include_role:
        name: app/assets/container/down
  when: app.assets.enabled
  tags: [assets]
