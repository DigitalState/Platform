---
- block:
    - name: Define discovery repository variables
      set_fact:
        discovery_repository: '{{ app.discovery.repository }}'
        discovery_version: '{{ app.discovery.version }}'
    - name: Download discovery repository
      include_role:
        name: app/discovery/repository/download
  when: app.discovery.enabled
  tags: [discovery]
