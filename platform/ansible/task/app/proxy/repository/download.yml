---
- block:
    - name: Define proxy repository variables
      set_fact:
        proxy_repository: '{{ app.proxy.repository }}'
        proxy_version: '{{ app.proxy.version }}'
    - name: Download proxy repository
      include_role:
        name: app/proxy/repository/download
  when: app.proxy.enabled
  tags: [proxy]
