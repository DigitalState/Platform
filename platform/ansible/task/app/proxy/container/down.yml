---
- block:
    - name: Define proxy container variables
      set_fact:
        proxy_env: '{{ platform.env }}'
    - name: Down proxy container
      include_role:
        name: app/proxy/container/down
  when: app.proxy.enabled
  tags: [proxy]
