---
- import_playbook: /etc/ansible/playbook/platform/vars/include.yml
- import_playbook: /etc/ansible/playbook/platform/vars/computed/include.yml

- name: Compile repository/branch/pull task list
  hosts: platform
  become: true
  tasks:
    - name: Compile files
      compile:
        app: '{{ app }}'
        directory: /etc/ansible/task/app/*
        task: repository/branch/pull
      register: files
  tags: [always]

- name: Pull repositories branch
  hosts: app
  become: true
  tasks:
    - name: Include compiled task list
      include_tasks: '{{ item }}'
      with_items: '{{ hostvars["platform"]["files"]["tasks"] }}'
      tags: [always]
