---
- import_playbook: /etc/ansible/playbook/platform/vars/include.yml

- name: Compile container/stop task list
  hosts: platform
  become: true
  tasks:
    - name: Compile files
      compile:
        app: '{{ app }}'
        directory: /etc/ansible/task/app/*
        task: container/stop
        order: desc
      register: files
  tags: [always]

- name: Stop containers
  hosts: app
  become: true
  tasks:
    - name: Include compiled task list
      include_tasks: '{{ item }}'
      with_items: '{{ hostvars["platform"]["files"]["tasks"] }}'
      tags: [always]
