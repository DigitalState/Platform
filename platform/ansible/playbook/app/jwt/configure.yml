---
- import_playbook: /etc/ansible/playbook/platform/vars/include.yml

- name: Compile jwt/configure task list
  hosts: platform
  become: true
  tasks:
    - name: Compile files
      compile:
        app: '{{ app }}'
        directory: /etc/ansible/task/app/*
        task: jwt/configure
      register: files
  tags: [always]

- name: Configure jwt settings
  hosts: app
  become: true
  tasks:
    - name: Include compiled task list
      include_tasks: '{{ item }}'
      with_items: '{{ hostvars["platform"]["files"]["tasks"] }}'
      tags: [always]
